interface SessionsGraphProps {
  data: any[]; // Replace 'any' with your actual session data type
}

const SessionsGraph = ({ data }: SessionsGraphProps) => {
  return <div>Dotgraph placeholder</div>;
};

export default SessionsGraph;
